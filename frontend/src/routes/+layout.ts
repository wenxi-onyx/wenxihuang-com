// Use 'auto' to let individual pages choose their prerender strategy
// Pages with `export const prerender = false` will be excluded
export const prerender = 'auto';

// Enable Server-Side Rendering for dynamic content
export const ssr = true;

// Enable Client-Side Rendering for interactivity
export const csr = true;
