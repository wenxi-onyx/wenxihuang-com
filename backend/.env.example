# Database connection URL
# For local development with Docker: postgresql://postgres:postgres@localhost:5433/postgres
# For Fly.io: Automatically set via flyctl postgres attach
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/postgres

# Session secret for cookie signing (REQUIRED, min 32 characters)
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a strong random value in production!
SESSION_SECRET=your-secret-key-change-in-production-min-32-chars

# Server port (optional, defaults to 8080)
PORT=8080

# CORS allowed origins (optional, comma-separated)
# Defaults to: https://wenxihuang.com,https://www.wenxihuang.com,https://wenxihuang-frontend.fly.dev,http://localhost:5173
# CORS_ORIGINS=https://example.com,http://localhost:5173

# Default admin password for initial setup (REQUIRED for initialize script)
# Used when running the initialize binary to create the default admin user
# IMPORTANT: Change this in production! Use a strong password.
# For Fly.io deployment, set this as a secret: flyctl secrets set ADMIN_PASSWORD="your-password" -a wenxihuang-backend
ADMIN_PASSWORD=change-me-in-production
